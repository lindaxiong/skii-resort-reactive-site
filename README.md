# Skii Resport Responsive Website
Created using CSS 2d and 3D animation. In this case, it's more efficient to leverage the browser’s CSS engine instead of hand-rolling animations, focused states, etc. 
